import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vue-ionic-sqlite',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
